import { writable } from 'svelte/store';

export const clientWidth = writable(0);
export const clientHeight = writable(0);

export const offsetHeight = writable(0);
export const offsetWidth = writable(0);

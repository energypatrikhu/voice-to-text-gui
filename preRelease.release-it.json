{
  "$schema": "https://unpkg.com/release-it@19/schema/release-it.json",
  "npm": {
    "publishPackageManager": "bun"
  },
  "git": {
    "tagAnnotation": "Pre-Release ${version}"
  },
  "gitlab": {
    "release": true,
    "preRelease": true,
    "tokenRef": "GITLAB_TOKEN",
    "origin": "https://gitlab.ep24.hu",
    "comments": {
      "submit": true,
      "issue": "_This issue has been resolved in v${version}. See [${releaseName}](${releaseUrl}) for release notes._",
      "pr": "_This merge request is included in v${version}. See [${releaseName}](${releaseUrl}) for release notes._"
    }
  }
}
